Source document (for thematic inspiration only — do NOT ask questions about its content):

```
{document}
```

Generate {n_per_seed} grade-school math word problem(s) with step-by-step solutions, following the rules in the system prompt. Return a JSON array.
