Seed text:

```
{seed}
```

Generate {n_per_seed} synthetic documents inspired by the seed, following all rules in the system prompt. Vary the document type (essay, news article, letter, court opinion, lecture, editorial, etc.) across your outputs. Return a JSON object with key "documents".
